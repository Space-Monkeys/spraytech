import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  SearchInput: {
    backgroundColor: '#FFF',
    width: '100%',
    height: 40,
    borderRadius: 8,
    paddingLeft: 15,
  },
});
